use Rand::Rng;
use Macroquad;
use term_size;
fn main() {
    let mut distance :u64 = 0;
    let mut food :u64 = 0; 
    let mut aliens :u64 = 0;
    let mut speed :u64 = 1;



    let Some((w,h)) = term_size::dimensions();
    println!(|"{} {}", w,h);

}
//sort cargo.toml 
//this is some new code that i want to save